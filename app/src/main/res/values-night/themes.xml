<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Android_Ozono" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_opaque</item>
        <item name="colorPrimaryDark">@color/blue_opaque</item>
        <item name="colorAccent">@color/blue</item>

        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/poppins</item>
        <item name="android:textSize">18sp</item>
        <item name="android:colorBackground">@color/background_night_color</item>
        <item name="android:textColorPrimary">@color/light_grey</item>
    </style>
</resources>